version: '3'

services:
    db:
        build: ./provision/sqlserver
        ports:
            - "11433:1433"
        environment:
            - ACCEPT_EULA=Y
            - SA_PASSWORD=a1waysB3Encrypting
        volumes:
            - ./volumes/mssql:/var/opt/mssql